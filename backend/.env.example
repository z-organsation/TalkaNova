# TalkaNova Backend - copy to .env and fill values
# Required: SECRET_KEY (min 32 chars; use: python -c "import secrets; print(secrets.token_hex(32))")

SECRET_KEY=your-secret-key-min-32-chars-change-in-production
DEBUG=false

# CORS (comma-separated; for Tor use your frontend origin)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database (default SQLite; prod: postgresql+asyncpg://user:pass@host/db)
# DATABASE_URL=sqlite+aiosqlite:///./talkanova.db

# Google OAuth (optional)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# Email (SMTP for password reset)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@talkanova.local
# FRONTEND_BASE_URL=http://localhost:3000

# Tor: set FRONTEND_BASE_URL to your .onion or use relative paths in emails
# RATE_LIMIT_PER_MINUTE=60
